# TRON MCP Server 环境配置

# ============ 私钥配置 ============

# TRON 私钥 (签名和广播交易时必需)
# 64 位十六进制字符串，不带 0x 前缀
# ⚠️ 请勿将真实私钥提交到版本控制！
# TRON_PRIVATE_KEY=your_64_hex_char_private_key_here

# ============ API 配置 ============

# TRONSCAN API URL (可选)
# 默认: https://nile.tronscan.org/api (Nile 测试网)
# 主网: https://apilist.tronscan.org/api
# TRONSCAN_API_URL=https://nile.tronscan.org/api

# TRONSCAN API KEY (可选)
# 若有配额限制，可填写
# TRONSCAN_API_KEY=your_tronscan_api_key

# 请求超时时间 (秒，可选，默认 10)
# REQUEST_TIMEOUT=10

# ============ 服务配置 ============

# SSE 模式端口 (可选，默认 8765)
# MCP_PORT=8765

# ============ 合约配置 ============

# USDT TRC20 合约地址 (可选)
# 默认: TXYZopYRdj2D9XRtbG411XZZ3kM5VkAeBf (测试网 Nile)
# 主网 Mainnet: TR7NHqjeKQxGTCi8q8ZY4pL8otSzgjLj6t
# USDT_CONTRACT_ADDRESS=TXYZopYRdj2D9XRtbG411XZZ3kM5VkAeBf

# USDT TRC20 合约地址 Hex 格式 (可选)
# 默认: 0x41d2b56458ed849f993432263dbc8e1da12013856a (测试网 Nile)
# USDT_CONTRACT_ADDRESS_HEX=0x41d2b56458ed849f993432263dbc8e1da12013856a

# ============ Gas 费用估算参数 ============

# USDT TRC20 转账预估能量消耗 (可选，默认 65000)
# 激活账户约 29000 Energy，未激活约 65000 Energy
# ESTIMATED_USDT_ENERGY=65000

# 每单位 Energy 的 SUN 价格 (可选，默认 420)
# ENERGY_PRICE_SUN=420

# TRX 转账最小 Gas 费用 (SUN，可选，默认 100000，约 0.1 TRX)
# MIN_TRX_TRANSFER_FEE=100000

# 每地址每天免费带宽点数 (可选，默认 600)
# FREE_BANDWIDTH_DAILY=600

# USDT 转账消耗的带宽字节数 (可选，默认 350)
# USDT_BANDWIDTH_BYTES=350

# 每单位带宽的 SUN 价格 (可选，默认 1000)
# BANDWIDTH_PRICE_SUN=1000
