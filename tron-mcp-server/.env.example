# ============================================================
# TRON MCP Server 环境配置
# 复制本文件为 .env 并填写你的配置
#   cp .env.example .env
# ============================================================

# ============ 网络选择 ============
# 设置为 mainnet (主网) 或 nile (Nile 测试网)
# 切换网络后，API URL 和合约地址会自动匹配，无需手动修改
# 默认: mainnet
TRON_NETWORK=mainnet

# ============ API Key 配置 (重要) ============

# TronGrid API Key (推荐填写)
# 用于交易构建和广播，无 Key 时有严格频率限制
# 申请地址: https://www.trongrid.io/
TRONGRID_API_KEY=

# TronScan API Key (推荐填写)
# 用于数据查询（余额、交易状态、Gas 参数、安全检查）
# 申请地址: https://tronscan.org/
TRONSCAN_API_KEY=

# ============ 私钥配置 (签名交易时必需) ============

# TRON 私钥，64 位十六进制字符串，不带 0x 前缀
# 用于 tron_sign_tx / tron_transfer 等需要签名的操作
# ⚠️ 请勿将真实私钥提交到版本控制！
# TRON_PRIVATE_KEY=

# ============ 高级配置 (一般无需修改) ============

# 自定义 TRONSCAN API URL (可选，切换网络时自动设置)
#   主网默认: https://apilist.tronscan.org/api
#   Nile 默认: https://nileapi.tronscan.org/api
# TRONSCAN_API_URL=

# 自定义 TronGrid API URL (可选，切换网络时自动设置)
#   主网默认: https://api.trongrid.io
#   Nile 默认: https://nile.trongrid.io
# TRONGRID_API_URL=

# 请求超时时间 (秒，可选，默认 10)
# REQUEST_TIMEOUT=10

# SSE 模式端口 (可选，默认 8765)
# MCP_PORT=8765

# 日志级别 (可选，默认 INFO)
# LOG_LEVEL=INFO

# ============ 合约配置 (可选，切换网络时自动设置) ============

# USDT TRC20 合约地址
#   主网默认: TR7NHqjeKQxGTCi8q8ZY4pL8otSzgjLj6t
#   Nile 默认: TXYZopYRdj2D9XRtbG411XZZ3kM5VkAeBf
# USDT_CONTRACT_ADDRESS=

# USDT TRC20 合约地址 Hex 格式 (可选)
# USDT_CONTRACT_ADDRESS_HEX=

# ============ Gas 费用估算参数 (可选) ============

# USDT TRC20 转账预估能量消耗 (默认 65000)
# 激活账户约 29000 Energy，未激活约 65000 Energy
# ESTIMATED_USDT_ENERGY=65000

# 每单位 Energy 的 SUN 价格 (默认 420)
# ENERGY_PRICE_SUN=420

# TRX 转账最小 Gas 费用 (SUN，默认 100000，约 0.1 TRX)
# MIN_TRX_TRANSFER_FEE=100000

# 每地址每天免费带宽点数 (默认 600)
# FREE_BANDWIDTH_DAILY=600

# USDT 转账消耗的带宽字节数 (默认 350)
# USDT_BANDWIDTH_BYTES=350

# 每单位带宽的 SUN 价格 (默认 1000)
# BANDWIDTH_PRICE_SUN=1000

# TRC20 转账 fee_limit (SUN，默认 100000000，即 100 TRX)
# TRONGRID_FEE_LIMIT=100000000
