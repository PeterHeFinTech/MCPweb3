# TRON MCP Server 环境配置

# GetBlock API Token (必填)
# 从 https://getblock.io/ 获取
GETBLOCK_API_TOKEN=your_api_token_here

# 自定义 API URL (可选)
# 如果设置，将覆盖默认的 GetBlock URL
# TRON_API_URL=https://api.trongrid.io/jsonrpc

# 请求超时时间 (秒，可选，默认 10)
# REQUEST_TIMEOUT=10
