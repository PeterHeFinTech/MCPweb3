# TRON MCP Server 依赖
# Python >= 3.10 recommended

# MCP SDK (核心)
mcp>=1.0.0

# HTTP 客户端
httpx>=0.24.0

# Base58 编码/解码 (用于 TRON 地址处理)
base58>=2.1.0

# TRON 密钥管理与交易签名
ecdsa>=0.18.0

# Keccak256 哈希 (用于地址派生)
pycryptodome>=3.19.0

# 环境变量加载
python-dotenv>=1.0.0

# SSE 模式依赖
uvicorn>=0.20.0
starlette>=0.27.0
sse-starlette>=1.0.0

# Pydantic (MCP 依赖)
pydantic>=2.0.0
pydantic-settings>=2.0.0

# QR Code 生成 (用于生成钱包地址二维码)
qrcode[pil]>=7.4.0

# 开发依赖 (可选)
pytest>=7.0.0
pytest-asyncio>=0.21.0
